{
  "manifest_version": 2,
  "name": "YouTube Lyrics Helper (AI)",
  "version": "1.0",
  "description": "Detects YouTube title, store approved lyrics sources, fetches pages, and uses a local LLM (Ollama) to extract lyrics.",
  "permissions": [
    "storage",
    "tabs",
    "http://localhost/*",
    "https://*/*"
  ],
  "content_scripts": [
    {
      "matches": ["*://*.youtube.com/watch*"],
      "js": ["content.js"],
      "css": ["styles.css"]
    }
  ],
  "background": {
    "scripts": ["background.js"]
  }
}
